apiVersion: apps/v1
kind: StatefulSet
metadata:
  name: influxdb
  namespace: murmetric
spec:
  serviceName: influxdb
  replicas: 1
  selector:
    matchLabels:
      app: influxdb
  template:
    metadata:
      labels:
        app: influxdb
    spec:
      containers:
        - name: influxdb
          image: influxdb:2.7
          # Poste le plus gourmand de la chaîne pendant un import massif (c'est
          # lui qui indexe et écrit les points), d'où le plafond le plus haut —
          # mais plafond tout de même : le nœud 4 cœurs est partagé avec
          # d'autres projets (MailFlow, MINER, ollama).
          resources:
            requests:
              cpu: 250m
              memory: 256Mi
            limits:
              cpu: 700m
              memory: 1Gi
          ports:
            - containerPort: 8086
          env:
            - name: DOCKER_INFLUXDB_INIT_MODE
              value: setup
            - name: DOCKER_INFLUXDB_INIT_USERNAME
              value: admin
            - name: DOCKER_INFLUXDB_INIT_PASSWORD
              valueFrom:
                secretKeyRef:
                  name: murmetric-secrets
                  key: influx-password
            - name: DOCKER_INFLUXDB_INIT_ORG
              value: FRD_CODEM
            - name: DOCKER_INFLUXDB_INIT_BUCKET
              value: Test_Capteurs
            - name: DOCKER_INFLUXDB_INIT_ADMIN_TOKEN
              valueFrom:
                secretKeyRef:
                  name: murmetric-secrets
                  key: influx-token
          volumeMounts:
            - name: influxdb-data
              mountPath: /var/lib/influxdb2
      volumes:
        - name: influxdb-data
          persistentVolumeClaim:
            claimName: influxdb-pvc
