# Dépendances MurMetric — PC labo Windows
# Cible : ingestion DeweSoftX (capteur de retrait)
# Installer avec : pip install -r requirements-windows.txt

# Publication MQTT vers le broker cloud
paho-mqtt>=2.1.0

# DSRemoteConnect : DLL fournie par DeweSoftX, wrappée via ctypes (stdlib Python)
# Copier DSRemoteConnect64.dll depuis : C:\ProgramData\DEWESoft\DSRemoteConnect\
# SQLite (store-and-forward résilience cloud) — stdlib Python, pas d'install
