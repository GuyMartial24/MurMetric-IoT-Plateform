# Dépendances MurMetric — Raspberry Pi (Linux)
# Cible : ingestion BLE (configure_capteurs + ingestion_capteurs_bluetooth)
# Installer avec : pip install -r requirements-rpi.txt
#
# Note : le bridge MQTT → InfluxDB tourne sur le VPS (docker-compose).
# influxdb-client n'est donc pas nécessaire sur le RPi.

# BLE scanning (capteurs Blue Maestro Disc Maxi)
bleak>=3.0.0

# Publication MQTT vers le broker cloud
paho-mqtt>=2.1.0

# Registre capteurs (mur/couche/position) interrogé via l'API de la webapp —
# source unique depuis le 13/08/2026 (cf. logique_projet.md section 32),
# capteurs.json local ne sert plus que de repli hors-ligne + champs
# techniques BLE (configure_capteurs.py).
requests>=2.31

# SQLite (store-and-forward résilience cloud) — stdlib Python, pas d'install
