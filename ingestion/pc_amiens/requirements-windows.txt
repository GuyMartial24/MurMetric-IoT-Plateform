# Dépendances MurMetric — PC labo Windows
# Cible : ingestion DeweSoftX par dépôt de fichiers .dxd (capteur de retrait)
# Installer avec : pip install -r requirements-windows.txt

# Publication MQTT vers le broker cloud
paho-mqtt>=2.1.0

# Registre capteurs_retrait (mur/couche/position) interrogé via l'API de la
# webapp — source unique depuis le 13/08/2026 (cf. logique_projet.md
# section 32), plus un fichier local géré à la main.
requests>=2.31

# Filtre de Hampel vectorisé (ingestion_dewesoft_dxd.py). Facultatif au sens
# strict — le code retombe automatiquement sur une version pure Python — mais
# fortement recommandé : la version NumPy est ~3x plus rapide et relâche le
# GIL pendant les calculs lourds, ce qui évite d'étouffer le thread réseau de
# paho sur les gros fichiers (cf. _filtrer_hampel_numpy).
numpy>=1.26

# SQLite (store-and-forward résilience cloud) — stdlib Python, pas d'install

# Import de fichiers .dxd exportés/déposés (ingestion_dewesoft_dxd.py).
# Pas de dépendance pip — DWDataReader_v5_0_8/ (SDK officiel DeweSoft, vendored)
# est lu en ctypes brut.
